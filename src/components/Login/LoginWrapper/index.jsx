import { Form } from "../Form";
import { Title } from "../Title";
import "./style.css";

export const LoginWrapper = () => {
  return (
    <div className="main">
      <Title />
      <Form />
    </div>
  );
};
